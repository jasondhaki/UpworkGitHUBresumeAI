# Runtime dependencies for the app itself. Pinned to what's actually running
# and tested in this build (see CONTEXT.md, 2026-09-01/02) -- versions matter
# here more than usual, since this project's own history is full of APIs
# (Gemini model names, Starlette's TemplateResponse signature, Docling's
# document structure) that changed meaningfully between versions.
fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
python-multipart==0.0.32
pydantic==2.13.5
httpx==0.28.1
python-dotenv==1.2.3
docling==2.124.0
