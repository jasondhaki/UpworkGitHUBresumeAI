# Only needed for generating test fixtures and running the browser-based
# integration tests in scripts/ -- not needed to run the app itself.
-r requirements.txt
playwright==1.62.0
fpdf2==2.8.8
python-docx==1.2.0

# After installing, Playwright also needs its browser binary:
#   python -m playwright install chromium
